enum EventCategory {
  meetup,
  sports,
  workshop,
  networking,
  food,
  creative,
  outdoor,
  fitness,
  learning,
  social
}

enum EventStatus { upcoming, live, ended, cancelled }

enum EventPrivacy { public, private }